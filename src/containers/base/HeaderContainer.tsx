import React from 'react';
import Header from '../../components/base/Header';

interface HeaderContainerProps {}
const HeaderContainer: React.FC<HeaderContainerProps> = () => <Header />;

export default HeaderContainer;
